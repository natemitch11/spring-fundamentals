package com.pluralsight.fundamentals.resources;

public class SpringBootCLI {
    /* To Initialize from the Command Line you need to have the springboot framework installed locally. This can be
    done in Scoop, Homebrew, or downloading the package from the zip. After it is installed, then you can perform
    many actions with the 'spring' prefix. 'spring --version' would should version.

    spring init    --- initializes a new project by calling out to spring initializr

    spring init --dependencies=dependency-1, dependency2, directoryName      --- defines dependencies

    spring run filename    ---- runs the file groovy or maven
     */
}
