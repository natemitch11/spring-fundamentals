package com.pluralsight.fundamentals.resources;

public class MVC {
    /*
    The dependency to develop using Model, Viewer, Controller project in Spring is called:
    spring-boot-starter-web
    and it imports Dispatcher servlet, error page, servlet container, jars

    Model- Representation of data within a system.
    View- displays the data.
    Controller- directing incoming user requests to the correct resources and then responds

    Controller accepts requests and maps those http requests to the service layer, the Service layer retrieves the
    data from the repository layer (data layer) and returns the info to the controller

    MAVEN REPACKAGES ALL RUNNABLE .jar FILES INTO ONE LARGER UBER JAR
    then it will find your main class and your main method as the entry point for the code
     */
}
